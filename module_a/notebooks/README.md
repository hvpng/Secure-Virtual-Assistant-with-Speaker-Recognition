# Module A notebooks

Training notebooks begin in milestone A2. Keep reusable implementation in
`module_a/src/`; notebooks should orchestrate that source rather than duplicate it.

A0/A1 intentionally provides no WavLM download, CAM++ implementation, training loop,
checkpoint, or GPU notebook.

